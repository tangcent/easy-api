rootProject.name = "easy-api"

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id("org.jetbrains.changelog") version "2.5.0"
    }
}
